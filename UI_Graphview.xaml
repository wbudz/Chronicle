<UserControl x:Class="Chronicle.UI_Graphview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="http://oxyplot.org/wpf" 
             xmlns:Chronicle="clr-namespace:Chronicle" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <StackPanel Height="60" DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBlock x:Name="tSelectedGraph" Margin="5" Text="(none selected)"/>
        </StackPanel>
        <oxy:PlotView x:Name="Plot" Model="{Binding}" >
        </oxy:PlotView>
    </DockPanel>
</UserControl>
